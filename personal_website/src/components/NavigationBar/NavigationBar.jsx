import './NavigationBar.css';
import { Logo } from '../Logo/Logo';

export const NavigationBar = () => {
  return (
    <div>
      <nav>
        <Logo theme='dark' color='teal' />
      </nav>
    </div>
  );
};
